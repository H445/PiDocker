IMAGE_NAME=pi-agent
IMAGE_TAG=latest
CONTAINER_NAME=pi-agent
VOLUME_NAME=pi-agent-data
# Optional: map host folders into the container (semicolon-separated host_path:container_path)
# VOLUME_MOUNTS=C:\Users\me\projects:/workspace;C:\Users\me\data:/data
# Optional: publish ports from container to host (semicolon-separated host:container[/tcp|udp])
# PORT_MAPPINGS=3000:3000;8080:80;5353:53/udp
